class ProfileUser {
  final String uid;
  final String email;
  final String displayName;
  ProfileUser({this.uid, this.email, this.displayName});
  //still need lng and lat and followers and trips
}
